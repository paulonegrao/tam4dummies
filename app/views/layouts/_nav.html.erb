<nav class="nav navbar-default">
  <div class="container">
      <ul class="nav navbar-nav">
        <!-- <li class="<#%= [root_path, lectures_path].include?(request.path) ? 'navlink open' : '' %>"><%= link_to "Home".html_safe, root_path %></li> -->
        <li class="<%= request.path.include?("root") ? 'navlink open' : '' %>"><%= link_to "Home".html_safe, root_path %></li>
      </ul>
      <ul class="nav navbar-nav">
        <li class="<%= request.path.include?("lectures") ? 'navlink open' : '' %>"><%= link_to "Lectures".html_safe, lectures_path %></li>
      </ul>
      <ul class="nav navbar-nav">
        <li class="<%= request.path.include?("tameline") ? 'navlink open' : '' %>"><%= link_to "TAMeline".html_safe, tameline_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              Hello <%= current_user.first_name %> <%= current_user.last_name %>! <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Log Out", sessions_path, method: :delete %></li>
            </ul>
          </li>
          <ul class="nav navbar-nav">
          </ul>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Let's Rock!<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Sign In", new_session_path %></li>
              <li><%= link_to "Sign Up", new_user_path %></li>
            </ul>
          </li>
        <% end %>
    <!-- </div> -->
  </div>
</nav>

      <div class="col-md-10"></div>
      <div class="col-md-2 offset">
        <%= link_to '', events_show_path, remote: true, id:'tam-event-keep-alive' %>
        <button id="on-off-air" class="tam-btonair off-air" data-live="0" disabled></button>
      </div>
