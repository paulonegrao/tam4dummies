if(@broadcast_id) {
  $("#broadcast_id").fadeIn(1200, function(){
        $(this).value("{@broadcast_id}");
        $(this).removeClass("hidden");
  });
  $("#share-screen").fadeIn(1200, function(){
        $(this).prop('disabled', false);
        $(this).css('opacity', 1);
   });
}
console.log('keep alive capturing');
